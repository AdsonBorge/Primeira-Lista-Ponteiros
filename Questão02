2. Seja o seguinte trecho de programa:

int i=3, j=5;
int *p, *q;
p = &i;
q = &j;

// Nas linhas 5 e 6, só é possivel a operação porque estamos armazenando o endereço das variaveis na 
dentro das variaveis 'p' e 'q'//

 Determine o valor das seguintes expressões

p == &i
Verdadeiro.

*p - *q:
-2.

**&p;
Desconheço, mas após a compilação resultado descoberto igual a 3. 

3-*q/*p+7: 
9,4.
